# Evidencia — Caso Práctico 2

Capturas requeridas (mínimo obligatorio según el enunciado):

| Archivo | Qué capturar |
|---|---|
| `01-catalogo.png` | Catálogo de libros con la disponibilidad visible (http://localhost:8080/libros). |
| `02-prestamo.png` | Formulario/pantalla de registrar préstamo logueado como `bibliotecaria1` (http://localhost:8080/prestamos/nuevo). |
| `03-403.png` | Un LECTOR (`lector1`) intentando entrar a una función de bibliotecario y viendo la página 403. |
| `04-postman-login.png` | Request de login en Postman (colección `biblioapp.postman_collection.json`). |
| `05-postman-api.png` | Request a la API de libros (`GET /api/libros`) en Postman con el JSON visible. |
| `06-atrasados.png` | Resultado de `GET /api/prestamos/atrasados` (requisito 5.3) — en Postman o en la sección roja de /prestamos. |

Credenciales: `bibliotecaria1` / `password123` (BIBLIOTECARIO),
`lector1` y `lector2` / `password123` (LECTOR).